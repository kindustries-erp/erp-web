/**
 * Chuẩn hóa tập trung các hằng số thời gian và độ trễ (Timing / Debounce Constants)
 * cho toàn bộ hệ thống Liouni ERP (erp-web).
 *
 * Bắt buộc sử dụng DEFAULT_DEBOUNCE_TIME làm giá trị mặc định cho toàn bộ search, input, filter và URL sync.
 */
export const DEFAULT_DEBOUNCE_TIME = 500; // 500ms
export const DEFAULT_DEBOUNCE_MS = DEFAULT_DEBOUNCE_TIME;
