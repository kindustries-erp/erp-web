# Task — Wave 2 BOM CRUD UI

## Request Input (bạn chỉ cần điền phần này)

- Type: FEATURE
- Mục tiêu: Wire data thật cho `ErpBomPage` theo pattern `MfgItems`, gồm list + create/edit drawer + lines editor + item lookup.
- Bối cảnh/ngữ cảnh: Wave 1 đã có shell/page stub/API client. Backend `GET /api/v1/bom` đã verify live trên `:10001`; FE cần đi tiếp Wave 2 theo plan `docs/tasks/20260607-erp-core-wave2-plan.md`.

## Goal

Biến `ErpBomPage` từ stub thành page CRUD thật cho BOM, reuse `PageLayout`, `DataTable`, `DrawerModal`, `Combobox`, `ActionDropdown`, và dùng `inventoryCoreApi.list({ search })` cho item lookup.

## Scope

- In-scope:
  - list BOM từ `bomCoreApi.list`
  - create/edit BOM bằng drawer
  - lines editor cho `componentItemId`, `qtyRequired`, `uom`, `scrapRate`
  - item lookup cho finished good và component item
  - smoke render + basic save flow handling ở UI layer
- Out-of-scope:
  - delete nếu BE chưa có endpoint
  - page khác ngoài BOM
  - backend contract changes trừ khi phát hiện blocker rõ ràng

## Relevant Files

- `src/pages/ErpBomPage.tsx` - page hiện đang là stub
- `src/pages/MfgItems.tsx` - pattern table/drawer/reference implementation
- `src/modules/bom-core/api/bomCoreApi.ts` - API client BOM
- `src/modules/inventory-core/api/inventoryCoreApi.ts` - item lookup
- `src/shared/components/DataTable.tsx` - table reuse
- `src/shared/components/DrawerModal.tsx` - drawer reuse
- `src/shared/components/Combobox.tsx` - item lookup reuse

## Gate 0 — DB Precheck (bắt buộc)

- Collections/fields liên quan: `erp_boms`, `erp_bom_lines`, `erp_inventory_items`
- Data nền cần có: BOM records để list; inventory items để lookup `finishedGoodItemId` và `componentItemId`
- Constraint/index/default cần có: BOM/BOM line records và item ids đã live từ phase 1 verify
- Kết quả: `DB_READY`
- Nếu `DB_GAP_FOUND`: link DB task (directus-staging):

## Checklist (bắt buộc cập nhật realtime)

- [x] 1.0 Gate 0 DB Precheck done
- [x] 2.0 Backend workflow/API gate done
- [x] 3.0 UI gate done
- [x] 4.0 Validation
  - [x] 4.1 Chạy `bunx tsc --noEmit`
  - [x] 4.2 Smoke test flow liên quan
- [ ] 5.0 Close
  - [ ] 5.1 Lessons learned entry (if issue)
  - [ ] 5.2 Commit + push code (web/api)
  - [ ] 5.3 Tổng kết evidence

## Validation Evidence

- DB precheck result: `DB_READY`
- `npx tsc --noEmit`: PASS
- Smoke test:
  - `bun run build` PASS
  - BOM list endpoint trên `:10001` trả record thật
  - BOM detail endpoint trên `:10001` trả `{ message, data { ..., lines[] } }`
  - inventory items endpoint trên `:10001` trả records thật cho item lookup
  - FE BOM page đã patch API client để unwrap đúng shape `message/data`

## Lessons Learned

- Không có issue / hoặc link entry: No issue

## Commit/Push Status

- Web repo:
- API repo: not affected unless blocker found
- DB/directus staging: apply+verify+document (no code push required)
