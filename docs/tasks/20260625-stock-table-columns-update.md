# Stock Table Columns Update

## Changes
- Separated the expand/collapse ChevronRight icon into its own dedicated `__expand` column.
- Removed the expand functionality from the `item_code` (SKU) column.
- Adjusted all main column widths from 120px to 140px.
- Renamed table ID to `inventory-stock-table-v3` to reset column visibility/order/sizing user preferences so the new layout and sizes apply correctly.

## Verification
- Verified columns are correctly sized.
- Verified new column order places expand icon next to the selection checkbox.
