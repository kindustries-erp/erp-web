# Multi-level BOM visualization in MO

## Scope

- Fetch from `/api/v1/production/explode-preview` instead of `bomCoreApi.get`.
- Flatten tree dynamically with `level` property.
- Update `DocumentLineTable` columns to indent based on `level`.
- Make sub-assemblies read-only.
